alias brew-out='brew update && brew outdated -g'
alias brew-up="brew update && brew upgrade -g && brew autoremove && brew cleanup --prune=all"
alias brew-clean="brew autoremove && brew cleanup --prune=all"
